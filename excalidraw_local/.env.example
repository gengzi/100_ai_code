# 服务器配置
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# 请求大小限制
MAX_REQUEST_SIZE=50mb
MAX_FILE_SIZE=10485760

# 渲染器池配置
RENDERER_POOL_SIZE=5
RENDERER_POOL_MIN_SIZE=1

# Redis缓存配置（可选）
REDIS_URL=redis://localhost:6379
CACHE_TTL=3600

# CORS配置
ALLOWED_ORIGINS=*

# 日志级别
LOG_LEVEL=info